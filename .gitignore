checkpoints/*.pt
checkpoints/*_epoch*/
checkpoints/mlm/
results/*.json
results/*.png
logs/*.out
logs/*.err
__pycache__/
*.py[cod]
.ipynb_checkpoints/
notebooks/*_executed.ipynb
.env
wandb/
.DS_Store
# Local-only: a full mirror of the trained-machine repo uploaded for analysis.
# Keep untracked so we never accidentally commit gigabytes of checkpoints.
GlocalDoc_trained/


# Local-only: multi-agent research swarm run artifacts (~59MB of prompt dumps).
# Re-runnable via `python -m swarm.orchestrator.run`; never commit the run outputs.
swarm/
