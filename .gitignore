checkpoints/*.pt
checkpoints/*_epoch*/
checkpoints/mlm/
results/*.json
results/*.png
logs/*.out
logs/*.err
__pycache__/
*.py[cod]
.ipynb_checkpoints/
notebooks/*_executed.ipynb
.env
wandb/
.DS_Store
# Local-only: a full mirror of the trained-machine repo uploaded for analysis.
# Keep untracked so we never accidentally commit gigabytes of checkpoints.
GlocalDoc_trained/

# Research swarm infrastructure + run outputs. The orchestrator and 81 persona
# YAMLs that produced the V2 blueprint live here (see swarm/README.md), along
# with per-run prompt/response logs that can be hundreds of MB. Kept local;
# the blueprint itself was distilled into the V2 code on this branch.
swarm/

# Local-only Claude Code overrides — currently arms the auto-memory-on-commit
# hook in .claude/hooks/update-memory-on-commit.sh. The script itself is
# committed; the toggle file is not (each clone opts in).
.claude/settings.local.json

